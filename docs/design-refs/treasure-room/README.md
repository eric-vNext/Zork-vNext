# Treasure Room — design reference drop

Pilot screen 3/3. See `docs/graphics-backlog.md` for the ask.

Drop whatever Claude Design produces here (images, exported frames,
palette/material notes). Claude Code will pick this up to reinterpret as
the `treasure` scene in `src/scenes/scenes.ts`.
